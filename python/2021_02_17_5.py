# python
# REturns the reverse of a string

def reverseString(string):
    return string[::-1]
# we set nothing in start, end -> This means that we can get whole string
# if we put step = -1 than we can get the reverse of a string


print(reverseString("grace"))
